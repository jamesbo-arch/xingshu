# 醒书日记微信小程序及管理后台PRD文档

> **版本修订记录**
>
> | 版本 | 日期 | 说明 |
> |------|------|------|
> | v1.0 | — | 初始版本 |
> | v2.0 | 2026-07-03 | 融入《醒书日记的会员转化策略设计》（doc/醒书日记的会员转化策略设计.md）：产品定位从"内容付费转化"调整为"价值观社区筛选"；新增游客→首篇创作五步引导流程、编辑精选、今日话题、会员内容墙（预览渐隐）、创作激励与荣誉体系、转化漏斗指标；支付方式修正为线下转账（与实现一致）；授权方式修正为手机号手动验证（与实现一致） |
> | v2.1 | 2026-07-03 | 小程序正式命名为"醒书"；新增"醒书活动"一级栏目（活动预告/报名/图文分享，详情与报名仅需微信授权、不强制手机号）；日记权限矩阵收紧：未授权用户仅可浏览日记列表，公众日记详情需已授权（手机号验证）后查看 |
> | v2.2 | 2026-07-03 | 分享裂变与推荐人关系：日记与活动均支持转发海报，海报右下角为**带参小程序码**（携带目标页 + 分享人 ID），扫码直达对应日记/活动页；新用户经分享码进入时系统将分享人记录为其**推荐人**（首次绑定、扫码不可覆盖）；管理后台用户信息增加推荐人展示，**管理员可人工修改用户的推荐人**（写审计日志） |

# 项目名称

**醒书**小程序及配套管理后台（小程序正式名称为"醒书"；"醒书日记"指小程序内的日记功能模块）

# 一、产品定位与目标

## 1.1 产品定位

醒书是一款以日记记录、分享互动与社群活动为核心的轻量化小程序，搭配高效管理后台。它不是普通的内容付费产品，而是一个**价值观社区**：通过日记与线上线下活动这两大载体，聚集"认真生活、自省向上、愿意付出"的人群。产品包含两大板块：**醒书日记**（日记编写、查询、检索、收藏、点赞、评论、转发，支持按需设置权限）与**醒书活动**（社群线上线下活动的预告、报名与图文分享）；管理后台则为运营者提供数据监控、用户管理、内容管控、活动管理与**社区运营（精选策展、话题引导、创作激励）**能力。

## 1.2 产品目标

- 用户端：打造安静、有质感、体验流畅的日记记录与互动平台，让用户在阅读中产生"这群人和我是一类人"的认同感，完成从读者到创作者、从创作者到会员的自然递进。

- 运营端：通过管理后台实现对用户、日记及互动数据的全方位管理，支持内容策展与转化漏斗监控，识别并沉淀高价值社区成员。

- 产品理念（v2 核心调整）：**会员机制的本质是"筛选"而非"转化"**。目标不是付费人数最大化，而是"让对的人愿意付费，让不对的人自然离开"。付费是加入圈子的投名状（第一道粗筛），付费后的社区行为（认真回应、鼓励新人、传递温暖）是第二道精筛。整体转化链路为三层漏斗：公域引流（公众号/视频号，用价值观吸引）→ 小程序承接（游客的"体验—认同—决策"）→ 会员沉淀（认同驱动付费）。

- 北极星先行指标：**授权用户→首篇创作转化率**。写过第一篇日记的用户，归属感与付费意愿显著高于纯阅读用户；付费转化是创作与归属的自然结果。

# 二、目标用户

## 2.1 核心用户

有日记记录习惯、乐于分享生活片段或心情感悟的人群，核心需求是便捷记录、安全存储、优质互动，隐私保护与内容个性化。

**目标人群画像（v2 补充）**：有爱心、热情、责任心、上进心、愿意付出的"认真生活者"。产品的引导、文案与筛选机制均围绕这一画像设计——吸引认同者，让无感者自然离开。此类人群对"套路"高度敏感，所有转化设计必须克制：不弹窗诱导、不制造焦虑（红点/角标）、不在体验早期推销会员。

## 2.2 次要用户

- 喜欢浏览他人生活分享、寻找情感共鸣的用户，核心需求是发现优质日记内容、参与互动交流。

- 有个性化记录需求（如私密日记、会员专属内容分享）的用户，核心需求是灵活设置内容权限，保障隐私安全。

## 2.3 运营用户

产品运营人员、内容审核人员，核心需求是高效管理用户与内容、监控互动数据、排查违规内容，保障产品正常运营。

# 三、核心功能需求

## 3.1 小程序核心功能

### 3.1.1 醒书广场模块

- 落地欢迎屏（v2 新增）：游客**首次**进入小程序时，先展示一个轻量欢迎过渡屏——顶部一句话定位（"在这里，有一群认真生活、知行合一的朋友。"）+ 一张当日编辑精选日记卡片。此屏**不放任何注册/开通按钮**，只放"进去看看"轻引导，3 秒内传递社区气质、降低防御心理。非首次进入直接落地广场列表。

- 编辑精选（v2 新增）：广场首屏前 5-8 篇为运营策展的"编辑精选"公众日记，选取标准是"最能体现自省、温暖、向上气质"而非"最热闹"。精选内容由管理后台维护（见 3.2）。精选区之后按时间流展示常规内容。

- 日记列表展示：展示所有符合用户权限的日记卡片，包含日记标题、作者、内容摘要、发布时间、标签、权限范围、点赞数、收藏数、评论数、转发数。卡片设计要求安静、留白、有质感，呼应"日记"的私密感。

- 日记标签可由管理员在后台维护，包括如：积极主动、以终为始、要事第一、双赢思维、移情聆听、统合综效、知止而后有定、修身为本、格物致知、君子慎独、诚中形外、十目所视、有斐君子、克明峻德、日新又新、听讼无讼、正心修身、絜矩之道、生财有道、以义为利。

- 日记多条件查询：支持按标题/内容关键词、标签、时间范围、作者昵称四种条件组合查询，提升内容查找效率。

- 日记详情查看：展示日记完整信息（标题、作者、全文、发布时间、标签、权限），支持点赞、收藏、评论、转发操作，返回列表时保持上次浏览位置，同步刷新互动数据。

- 日记点赞/取消点赞：用户可对日记进行点赞，再次点击取消点赞，互动数据实时同步。

- 日记收藏/取消收藏：用户可收藏日记，收藏状态实时切换，收藏内容可在“我的收藏”页面查看。

- 日记评论与回复：支持一级评论（对日记本身）和二级回复（对评论的回复），用户可删除自己发布的评论/回复。

- 日记转发（v2.2 增强）：支持生成本日记专属海报，右下角为**带参小程序码**（scene 携带日记 ID + 分享人用户 ID），扫码直达该日记详情页；便于用户分享至第三方平台。分享行为参与推荐人关系绑定（见 3.1.5）。

- 新建日记：右下角悬浮笔形图标，点击进入编写页面，支持输入标题、内容、配图、选择标签、设置权限（公众/会员/自己），提交后同步至日记列表。

- 今日话题（v2 新增）：新建日记页顶部提供 3-4 个当日引导话题（如"今天有什么让你心头一暖的小事？""最近你为别人做过的一件事"），点选话题即预填为写作引子，也可自由书写。话题内容由管理后台维护，方向上引导"温暖、付出、自省"的内容气质。目的：消除空白页恐惧，降低首篇创作门槛。

- 首篇创作特殊规则（v2 新增，转化漏斗的临门一脚）：
    - 用户的**第一篇**日记：标题可选（一句话也能发布），突破"标题+内容必填"的常规校验；
    - 首篇默认权限为"自己"（仅自己可见），降低"被人看到"的心理压力；发布后温柔提示"如果愿意分享，可以改成公开，让同路人看到"；
    - 输入框提示语："不用华丽，真实就好。哪怕只有一句话。"；
    - 写作过程中字数达到一定量时给予一次微鼓励文案（如"写得真好，继续"），不使用打断式弹窗；
    - 首篇发布成功展示"高光时刻"反馈页（见 5.1.13）。

### 3.1.2 我的收藏模块

- 收藏日记列表：默认展示当前用户收藏的所有日记，页面样式与醒书广场一致，包含完整日记卡片信息。

- 收藏管理：支持在列表页或日记详情页取消收藏，取消后实时从收藏列表中移除，同步更新互动数据。

- 筛选与查询：支持沿用醒书广场的查询条件（标题/内容、标签、时间、作者），快速筛选收藏中的目标日记。

### 3.1.3 我的日记模块

- 个人日记列表：默认展示当前用户发布的所有日记，页面样式与醒书广场一致，包含完整日记卡片信息。

- 日记操作：支持新增日记（跳转至新建页面）、编辑自己的日记（修改标题、内容、标签、权限）、删除自己的日记（删除后不可恢复，同步删除相关互动数据，后台为逻辑删除）。

- 筛选与查询：支持沿用醒书广场的查询条件，快速筛选个人发布的目标日记。

### 3.1.4 会员中心模块

- 用户身份展示：明确显示当前用户身份（未授权手机号游客/已授权手机号游客/会员）。

- 权益查询：展示当前身份对应的权限的权益（如可查看的日记权限、可使用的功能），会员额外展示专属权益。

- 互动统计：展示用户发布日记的总数量、总点赞数、总收藏数、总评论数、总转发数，直观呈现内容影响力。

- 会员套餐：年卡 365 元/年（唯一套餐），强调"一年的陪伴与承诺"。付费方式：线下转账 + 管理员确认激活（与当前实现一致，不接入微信在线支付）。（策略文档提出的"月卡 30 元体验入口"为后续迭代备选项，暂不实施。）

- 付费定位文案（v2 新增，"投名状"策略）：会员页明确传递——"会员费不只是看内容，更是你对这份认真的承诺。我们希望聚集真正愿意付出的人。"把付费的意义从"消费"升华为"认同"，让付费本身成为筛选机制。

- 会员身份感（v2 新增）：会员专属标识/头像框在日记卡片与评论区可见；后续迭代规划：会员优先评论位、专属"醒友圈"社群、年度阅读/创作报告。

- 会员管理：会员用户展示会员有效期、剩余天数；非会员用户提供会员购买入口，支持跳转至购买页。

- 权限管控：根据用户身份限制功能使用，未授权用户在互动触发时引导手机号验证；会员解锁会员权限日记完整阅读（非会员见"预览 30% + 渐隐"，见 3.1.5）。

### 3.1.5 通用功能

- 用户授权（v2 调整触发时机与定位，v2.1 补充触发点）：**不主动弹出授权**。当游客产生兴趣或互动冲动（**点击日记卡片查看详情**/点赞/收藏/评论/新建日记）的瞬间才触发手机号验证——这是情绪与认同的最高点，转化率最高。授权定位从"解锁功能"重构为"加入一群人的仪式"（文案见 5.1.12）。授权方式为手机号手动输入验证（与当前实现一致）。

- 授权层级（v2.1 新增）：产品存在两级授权，勿混淆：
    - **微信授权**（轻）：拉起小程序基础授权，不涉及手机号——用于醒书活动的详情查看与报名；
    - **手机号验证**（重）：将"未授权用户"升级为"已授权用户"——用于日记详情阅读与全部互动功能。
    - 用户身份维持三态不变：未授权 / 已授权非会员 / 已授权会员。

- 授权欢迎时刻（v2 新增）：授权成功后不直接跳回原页面，先展示一个短暂的欢迎页："🌿 欢迎你，第 X 位醒书人"（X 为真实注册序号）+ 社区价值观一句话 + 两个出口按钮：轻动作「给刚才那篇日记点个赞」与重动作「写下我的第一篇」（视觉上更突出，埋下创作种子）。详见 5.1.14。

- 数据同步：所有互动操作（点赞、收藏、评论、转发）、日记操作（新增、编辑、删除）的数据实时同步，确保小程序与管理后台数据一致。

- 隐私保护与日记权限矩阵（v2.1 收紧）：日记维持三种权限（公众/会员/私密），三种身份的可见性如下：

    | 身份 \ 日记权限 | 公众日记 | 会员日记 | 私密日记 |
    |----------------|----------|----------|----------|
    | 未授权用户 | 仅列表卡片（标题/摘要），**点击详情触发手机号验证引导** | 仅列表卡片 | 不可见 |
    | 已授权非会员 | **详情全文** | 详情预览前约 30% + 渐隐（软筛选，见下） | 不可见 |
    | 已授权会员 | 详情全文 | **详情全文** | 不可见 |
    | 作者本人 | 详情全文 | 详情全文 | 详情全文 |

    会员日记渐隐规则：已授权非会员打开会员日记详情，展示开头约 30% 后渐隐，渐隐处叠加提示"完整内容向会员开放"+ 开通入口，转化被内容打动的人。渐隐截断在服务端完成，接口返回不包含被截断内容。私密日记严格仅作者可见（列表中对他人不展示）。

- 克制原则（v2 新增，v2.1 修订）：任何页面不得出现"注册领福利"式弹窗、不得在浏览早期提示开通会员、不得使用红点/角标制造焦虑。未授权用户可自由浏览日记列表（标题与摘要），列表本身即"钩子区"；点击详情时以邀请式文案引导验证（见 5.1.12），不使用强制拦截式弹窗。

- 分享裂变与推荐人关系（v2.2 新增）：
    - **带参小程序码**：日记海报与活动海报的二维码均为微信带参小程序码（`generateMiniCode` 云函数生成），scene 参数携带 `目标类型 + 目标ID + 分享人用户ID`；扫码打开小程序后直达对应日记详情页/活动详情页。
    - **推荐人绑定**：扫码进入的用户若为**新用户**（首次登录创建账号），系统自动将 scene 中的分享人记录为其推荐人（`users.referrer_user_id`）。绑定规则：仅首次有效、后续扫其他人的码不覆盖；分享人不能是自己；老用户扫码只跳转不改变推荐关系。**扫码路径不可覆盖，但管理员可在后台人工修改推荐人**（用于纠错与线下场景补录，见 3.2）。
    - **用途**：推荐关系沉淀在用户档案中，管理后台可见；作为后续运营激励（如推荐达人荣誉、赠会员时长）的数据基础（激励规则属 M6 迭代，本期只做关系记录）。

### 3.1.6 创作激励与荣誉体系（v2 新增）

核心逻辑：要筛选"愿意付出的人"，就让付出本身被看见、被奖励。会员由此形成两个来源——付费的认同者 + 被奖励的贡献者，两者都是目标人群。

- 官方首篇回应（性价比最高的投入）：每位新人的首篇日记，官方账号须在 1 小时内点赞 + 留一句真诚评论（真人运营或机制化温暖回复），让用户的"第一次付出"立刻被看见。

- 徽章与荣誉：「连续记录」徽章（按连续创作天数分级）、「被收藏最多」等荣誉标识，在个人主页与日记卡片展示。

- 会员时长奖励：优质创作者可获赠会员时长——让"愿意付出"的人不付费也能成为会员。评定由运营在管理后台执行。

- 付出型互动观察（第二道精筛）：管理后台沉淀行为数据——谁在认真回应他人日记、谁在持续鼓励新人、谁的内容在传递温暖；据此授予「荣誉会员」「社区共建者」等更高身份（后续迭代）。

### 3.1.7 醒书活动模块（v2.1 新增）

分享醒书社群的线上线下活动信息，是价值观社区从线上内容走向真实连接的载体。

- 活动列表：按状态分组展示活动卡片——**近期预告**（含报名入口）、**进行中**、**往期回顾**。卡片包含：活动封面图、标题、时间、形式（线上/线下+城市）、报名状态（可报名/名额已满/已截止）、已报名人数。

- 活动详情：图文详情（富文本+图片）、时间地点、组织方、报名须知、报名按钮、已报名人数/名额上限。**查看详情需完成微信授权（不要求手机号验证）**——未授权用户点击活动卡片时拉起小程序微信授权。

- 活动报名：微信授权后即可报名（**不强制手机号**）；报名表单字段由运营按活动配置（如称呼、联系方式选填）；报名成功展示确认信息，支持取消报名；名额满后展示候补或关闭报名（运营配置）。

- 活动图文分享（往期回顾）：活动结束后由运营发布图文回顾（照片墙 + 文字记录），所有用户可浏览，传递社区真实温度，反哺认同感与会员转化。

- 活动海报分享（v2.2 新增）：活动详情页提供"分享"入口，生成活动专属海报（封面、标题、时间地点、报名状态），右下角为带参小程序码（scene 携带活动 ID + 分享人用户 ID），扫码直达该活动详情页；推荐人绑定规则同 3.1.5。

- 权限说明：活动列表对所有身份可见；详情与报名仅需微信授权（轻授权），与日记的手机号验证（重授权）相互独立，互不阻塞。

- 首发范围划分（v2.1 确认：活动模块首发必须有，但第一阶段从简）：
    - **首发 MVP**：活动列表（近期预告 / 往期回顾两种状态）、活动详情（文字分段 + 图片，非富文本）、一键报名/取消（报名表单固定为"称呼（必填）+ 联系方式（选填）"两个字段，不做可配置）、名额上限与已报名人数展示、**活动海报分享（带参小程序码 + 推荐人绑定，v2.2）**、后台活动创建/编辑/上下线 + 报名名单查看；
    - **后续迭代**：可配置报名表单、候补机制、核销签到、"进行中"独立状态、富文本详情编辑器、活动开始提醒、报名名单导出 Excel、推荐激励规则。

## 3.2 管理后台核心功能

- 用户管理：查看所有用户信息（用户ID、手机号、身份类型、会员有效期、注册时间、**推荐人**（v2.2，经分享码进入时绑定的分享人）），支持按身份、注册时间筛选用户；用户详情页展示"他推荐的用户"列表；**管理员可修改用户的推荐人**（从用户列表中选择新推荐人或清空，校验：推荐人不能是用户本人、不得形成互为推荐的循环，操作写入 admin_logs 审计日志）。

- 日记管理：查看所有用户发布的日记信息（完整内容、作者、发布时间、标签、权限、互动数据），支持按标题、内容、标签、时间范围、作者、权限、互动数据等条件组合查询。

- 互动数据管理：查看每篇日记的详细互动数据（点赞列表、收藏列表、评论/回复列表、转发记录），支持查看单条互动的具体信息（如评论者、评论时间）。

- 内容管控：支持查看日记详情，对违规日记进行删除操作，删除后同步删除小程序端内容及相关互动数据。

- 运营管理（v2 新增）：
    - 编辑精选管理：设置/调整广场首屏精选日记（5-8 篇）及排序，可预设"当日金句"用于落地欢迎屏；
    - 今日话题管理：维护创作引导话题库（增删改、排期），话题方向对齐"温暖、付出、自省"；
    - 新人首篇待回应队列：列出 1 小时内待官方回应的新人首篇日记，支持快捷点赞 + 评论；
    - 荣誉与激励管理：授予徽章/荣誉身份、赠送会员时长（操作写入审计日志）。

- 活动管理（v2.1 新增）：
    - 活动发布：创建/编辑活动（标题、封面、图文详情、时间、地点/线上链接、名额上限、报名表单字段、报名截止时间），支持上下线；
    - 报名管理：查看/导出各活动报名名单，支持手动核销签到；
    - 活动回顾：活动结束后发布图文回顾（照片 + 文字），关联原活动。

- 数据统计：直观展示平台总用户数、会员数、日记总数、总互动数（点赞、收藏、评论、转发），支持按时间范围查看数据变化。

- 转化漏斗监控（v2 新增）：跟踪五个核心转化节点——①游客阅读时长与深度；②游客→授权用户转化率；③**授权用户→首篇创作转化率（北极星指标）**；④创作者→付费会员转化率；⑤会员续费率。以漏斗图呈现，支持按时间范围对比。

# 四、信息架构与页面结构

## 4.1 小程序信息架构

### 一级导航（底部Tab栏，v2.1 调整为 5 个）

1. 醒书广场（默认首页）

2. 醒书活动（v2.1 新增）

3. 我的收藏

4. 我的日记

5. 会员中心

### 二级页面（各导航对应子页面）

- 醒书广场

    - 日记列表页

    - 日记查询页（筛选弹窗/独立页面）

    - 日记详情页（内嵌点赞、评论、收藏、转发等功能）

    - 新建日记页

    - 日记转发海报页（弹窗，可下载成本地图片，也可直接转发微信）

- 醒书活动（v2.1 新增）

    - 活动列表页（近期预告 / 进行中 / 往期回顾）

    - 活动详情页（图文详情 + 报名，需微信授权）

    - 活动报名表单页/弹窗

    - 活动回顾图文页

- 我的收藏

    - 收藏日记列表页

    - 日记详情页（与广场详情页一致）

- 我的日记

    - 个人日记列表页

    - 日记详情页（与广场详情页一致）

    - 新建日记页（与广场新建页一致）

    - 编辑日记页（复用新建页布局，填充原有内容）

- 会员中心

    - 会员信息与权益页

    - 会员有效期查询

    - 互动统计页

    - 会员购买页（支持iphone、安卓、鸿蒙体系的支付操作）

- 通用页面

    - 落地欢迎屏（v2 新增，游客首次进入）

    - 手机号授权页（v2 文案与触发时机重构）

    - 授权欢迎时刻页（v2 新增）

    - 首篇发布高光时刻页/弹窗（v2 新增）

    - 提示弹窗（权限不足、操作成功/失败等）

## 4.2 管理后台信息架构

### 一级导航（左侧菜单栏）

1. 首页（数据概览，含转化漏斗）

2. 用户管理

3. 日记管理

4. 互动数据管理

5. 运营管理（v2 新增：编辑精选 / 今日话题 / 新人首篇回应 / 荣誉激励）

6. 活动管理（v2.1 新增：活动发布 / 报名管理 / 活动回顾）

### 二级页面

- 首页（数据概览）：平台核心数据统计图表、关键指标展示

- 用户管理

    - 用户列表页（展示所有用户信息）

    - 用户详情页（展示单个用户的完整信息、发布的日记、互动记录）

- 日记管理

    - 日记列表页（展示所有日记信息）

    - 日记详情页（展示日记完整内容、互动数据）

- 互动数据管理

    - 点赞数据页（按日记/用户筛选点赞记录）

    - 收藏数据页（按日记/用户筛选收藏记录）

    - 评论数据页（按日记/用户筛选评论/回复记录）

    - 转发数据页（按日记/用户筛选转发记录）

# 五、页面说明（逐页说明）

## 5.1 小程序页面说明

### 5.1.1 醒书广场 - 日记列表页

- 页面布局：顶部搜索/筛选栏 + 中间日记卡片列表 + 右下角悬浮新建图标（笔形）。

- 顶部区域：左侧显示“醒书广场”标题，右侧设置筛选按钮，点击弹出筛选弹窗；搜索框支持输入关键词（标题/内容），回车触发搜索。

- 日记卡片：统一卡片样式，自上而下展示：标题（加粗，最多2行，超出省略）、作者（小字，灰色）、内容摘要（最多3行，超出省略）、发布时间+标签（标签为圆角矩形，浅底色，可多个并排）、权限标识（公众/会员/自己，图标+文字提示）、底部互动栏（点赞数+收藏数+评论数+转发数，搭配对应图标）。

- 悬浮图标：固定在右下角，笔形图标，点击跳转至新建日记页，点击时有轻微震动反馈。

- 交互说明：列表支持下拉刷新、上拉加载更多；点击日记卡片跳转至详情页（v2.1：未授权用户点击卡片时先进入手机号验证引导，验证成功后直达该日记详情）；筛选弹窗确认后，列表实时刷新并定位至顶部；未授权用户点击新建/点赞/收藏/评论/转发按钮，跳转至手机号验证页。

### 5.1.2 醒书广场 - 日记查询筛选弹窗

- 弹窗布局：弹出式弹窗（从底部上滑），包含4个筛选条件：关键词（标题/内容）、标签、时间范围、作者，底部设置“重置”和“确认”按钮。

- 筛选条件：

    - 关键词：输入框，支持模糊查询。

    - 标签：下拉选择框，支持多选，可展示平台所有标签。

    - 时间范围：日期选择器，支持选择开始时间和结束时间，默认无限制。

    - 作者：输入框，支持输入作者昵称模糊查询。

- 交互说明：点击“重置”，清空所有筛选条件；点击“确认”，关闭弹窗并刷新日记列表，展示符合条件的内容；筛选条件可单独选择或组合选择，无选择时默认展示所有符合用户权限的日记。

### 5.1.3 醒书广场 - 日记详情页

- 页面布局：顶部返回按钮 + 中间日记内容区域 + 底部互动栏 + 评论列表区域。

- 顶部区域：左侧返回按钮（点击返回列表页，保持上次浏览位置），右侧无其他操作；标题居中（加粗，换行展示）。

- 日记内容区域：自上而下展示：作者（小字，灰色）、发布时间+标签+权限标识（与列表页一致）、完整日记内容（换行展示，支持分段）。

- 底部互动栏：固定在内容下方，从左至右依次为：点赞按钮（图标+数量，点赞后图标变色）、收藏按钮（图标+数量，收藏后图标变色）、评论按钮（图标+数量）、转发按钮（图标+数量），点击对应按钮触发对应操作。

- 评论列表区域：互动栏下方，展示所有一级评论，每条评论包含：评论者昵称、评论内容、评论时间、回复按钮（点击弹出回复输入框）；点击回复按钮，显示二级回复列表，支持输入回复内容并提交，可删除自己的评论/回复。

- 交互说明：点赞/收藏后，实时更新数量和按钮状态；点击评论按钮，聚焦至评论列表并弹出评论输入框；点击转发按钮，跳转至海报生成页；返回列表页时，列表自动刷新互动数据，并定位至上次进入详情页的位置；未授权用户触发操作时，跳转至授权页。

### 5.1.4 醒书广场 - 新建日记页

- 页面布局：顶部标题+保存按钮 + 中间输入区域 + 底部权限/标签设置区域。

- 顶部区域：左侧返回按钮（点击取消新建，弹出确认弹窗），中间“新建日记”标题，右侧“保存”按钮（默认置灰，输入标题和内容后激活）。

- 今日话题区（v2 新增，输入区上方）：“今天，想记录点什么？（选一个开始，或自由书写）”+ 3-4 个话题卡片（来自管理后台话题库）+ [自由书写] 选项；点选话题后预填为写作引子。

- 输入区域：

    - 标题输入框：提示文字“请输入日记标题”，最多输入30字，超出提示；**用户首篇日记时标题可选**。

    - 内容输入框：提示文字（首篇）“不用华丽，真实就好。哪怕只有一句话。”/（常规）“请输入日记内容”，支持多行输入、分段，无字数限制，显示输入字数统计；写作中字数达到阈值给予一次非打断式微鼓励。

- 底部设置区域：

    - 标签设置：提示文字“请选择日记标签”，支持多选，可自定义新增标签（需审核，暂不展示给其他用户）。

    - 权限设置：提示文字“请设置日记权限”，单选按钮，三个选项：公众（所有人可见）、会员（仅会员可见）、自己（仅自己可见），默认选择“公众”。

- 交互说明：输入标题和内容后，“保存”按钮激活，点击保存，提交日记并跳转至日记列表页（我的日记列表），提示“发布成功”；未授权用户点击保存，跳转至授权页；取消新建时，若已输入内容，弹出“是否放弃编辑”确认弹窗，确认后返回上一页。

### 5.1.5 醒书广场 - 日记转发海报页

- 页面布局：顶部返回按钮 + 中间海报展示区域 + 底部保存/分享按钮。

- 海报展示区域：居中展示日记专属海报，海报包含：日记标题、作者昵称、日记内容摘要（1-2行）、右下角**带参小程序码**（v2.2：scene 携带日记 ID + 分享人用户 ID，扫码直达该日记详情页并触发推荐人绑定，见 3.1.5）、产品Logo。

- 底部按钮：左侧“保存图片”按钮（点击保存海报至手机相册，提示“保存成功”），右侧“分享”按钮（点击弹出分享选项，支持分享至微信好友、朋友圈等）。

- 交互说明：海报生成后自动展示，加载过程中显示加载动画；点击返回按钮，返回日记详情页；保存图片需获取相册权限，未授权时提示引导授权。

### 5.1.6 我的收藏 - 收藏日记列表页

- 页面布局：与醒书广场日记列表页完全一致（顶部搜索/筛选栏 + 中间日记卡片列表 + 右下角悬浮新建图标）。

- 核心差异：默认展示当前用户收藏的日记，筛选/查询条件仅作用于收藏列表；日记卡片权限标识仍显示原日记权限，不影响收藏查看（自己收藏的会员/私密日记，自己可查看）。

- 交互说明：与醒书广场列表页一致，额外支持：点击收藏按钮取消收藏，取消后实时从列表中移除，提示“已取消收藏”；取消收藏后，同步更新日记详情页及广场列表页的收藏数据。

### 5.1.7 我的日记 - 个人日记列表页

- 页面布局：与醒书广场日记列表页一致，额外在日记卡片右侧增加“编辑”“删除”按钮（仅自己可见）。

- 核心差异：默认展示当前用户发布的所有日记，筛选/查询条件仅作用于个人日记；日记卡片右侧编辑/删除按钮，点击编辑跳转至编辑日记页，点击删除弹出确认弹窗。

- 交互说明：与醒书广场列表页一致，额外支持：

    - 编辑：跳转至编辑页面，填充原有日记内容、标签、权限，修改后保存，提示“编辑成功”，同步更新所有相关页面数据。

    - 删除：确认删除后，日记及相关互动数据（点赞、收藏、评论、转发）全部删除，提示“删除成功”，列表实时刷新。

### 5.1.8 我的日记 - 编辑日记页

- 页面布局：与新建日记页完全一致，仅顶部标题改为“编辑日记”。

- 核心差异：输入区域、标签、权限均填充原有日记内容，支持修改所有字段；标签修改后，原标签自动取消，新标签生效；权限修改后，实时影响日记可见范围。

- 交互说明：与新建日记页一致，保存后跳转至个人日记列表页，提示“编辑成功”；取消编辑时，若有修改，弹出确认弹窗；未授权用户无法进入编辑页面，跳转至授权页。

### 5.1.9 会员中心 - 会员信息与权益页

- 页面布局：顶部用户身份展示区 + 中间权益展示区 + 底部操作/统计入口。

- 用户身份展示区：居中展示用户头像（默认头像，可后续扩展上传）、昵称、身份标签（未授权手机号游客/已授权手机号游客/会员）；会员额外展示会员有效期、剩余天数（如“会员有效期至：2027-02-24，剩余360天”）。

- 权益展示区：按身份展示对应权益，用卡片样式呈现，每条权益搭配图标+文字：

    - 未授权手机号游客：可查看公众权限日记；需授权手机号解锁编写、点赞、收藏、评论、转发功能。

    - 已授权手机号游客：可查看公众权限日记；可使用编写、点赞、收藏、评论、转发功能；不可查看会员权限日记。

    - 会员：可查看公众、会员权限日记；可使用所有功能；享受会员专属标识（日记卡片、个人主页）。

- 底部操作/统计入口：两个按钮，“互动统计”（跳转至统计页）、“开通/续费会员”（非会员显示“开通会员”，会员显示“续费会员”，跳转至购买页）。

- 交互说明：未授权用户点击“开通会员”“互动统计”，跳转至授权页；会员点击“续费会员”，跳转至购买页；点击身份标签，弹出身份说明弹窗。

### 5.1.10 会员中心 - 互动统计页

- 页面布局：顶部标题“我的互动统计” + 中间数据卡片区域 + 底部数据明细入口。

- 数据卡片区域：四个卡片，分别展示：发布日记总数、总点赞数、总收藏数、总评论数+总转发数，每个卡片包含图标、数据、单位，卡片样式统一，色彩区分。

- 底部明细入口：“查看详细数据”按钮，点击跳转至数据明细页（展示每篇日记的具体互动数据）。

- 交互说明：数据实时同步，支持下拉刷新更新数据；未授权用户无法进入，跳转至授权页。

### 5.1.11 会员中心 - 会员购买页（v2 调整：投名状文案 + 线下转账）

- 页面布局：顶部标题“加入醒书会员” + 价值观文案区 + 中间会员套餐区域 + 底部转账指引区域。

- 价值观文案区（投名状定位）：“会员费不只是看内容，更是你对这份认真的承诺。我们希望聚集真正愿意付出的人。”

- 会员套餐区域：唯一套餐卡片——年卡（365元/年，标注“一年的陪伴与承诺”）：可查看会员权限日记全文、会员专属标识、后续会员权益，开通后 1 年有效。

- 底部转账指引区域：展示线下转账流程（添加运营微信 → 备注昵称与手机号 → 转账 → 管理员确认后激活），含运营联系方式卡片。

- 交互说明：管理员在后台确认收款并激活（activateMember），激活后用户端会员身份与有效期实时更新；未授权用户进入时先引导手机号验证。

### 5.1.12 通用页面 - 手机号验证页（v2 文案重构：从“解锁功能”到“加入仪式”）

- 页面布局：居中展示邀请文案 + 手机号输入区 + 协议勾选 + 底部承诺文字。

- 邀请文案（替代 v1 的“请授权手机号，解锁更多功能”）：
    标题：“成为醒书的一员”
    正文：“你刚刚被这篇日记打动了，对吗？留下你的身份，你就能为喜欢的文字点赞、收藏，也能开始记录属于你自己的故事。”
    （若非互动触发进入，正文退化为通用版：“留下你的身份，加入一群认真生活的人。”）

- 验证方式：手机号手动输入 + 协议勾选（与当前实现一致）。

- 底部承诺文字：“我们承诺：你的信息仅用于身份识别，绝不外泄。”（小字，灰色，居中）。

- 交互说明：验证成功后**进入欢迎时刻页（5.1.14）**，再返回之前触发操作的页面并自动完成原互动动作；验证失败提示原因，可重试；用户身份更新为“已授权手机号游客”。

### 5.1.13 通用页面 - 首篇发布高光时刻（v2 新增）

- 触发条件：用户发布人生中**第一篇**日记成功后展示（全屏轻量页或弹窗）。

- 内容：“✨ 你的第一篇日记，诞生了 / 这是属于你的起点。从今天起，你也是认真生活的记录者之一。”+ 两个按钮：[ 看看它的样子 ]（跳转日记详情）、[ 再写一篇 ]（返回新建页）。

- 联动机制：首篇发布事件进入管理后台“新人首篇待回应队列”，官方账号须在 1 小时内点赞 + 真诚评论（见 3.1.6）。

- 交互说明：仅首篇触发一次；页面氛围温暖克制，无任何会员推销元素。

### 5.1.14 通用页面 - 授权欢迎时刻页（v2 新增）

- 触发条件：手机号验证成功后、返回原页面前展示。

- 内容：“🌿 欢迎你，第 X 位醒书人”（X 为真实注册序号）+ “这里没有点赞数的攀比，没有喧嚣的表演。我们相信：认真记录的人，值得被认真对待。”+ 两个出口按钮：[ 给刚才那篇日记点个赞 ]（轻动作，完成原互动）、[ 写下我的第一篇 ]（重动作，视觉上更突出，跳转新建日记页并带出今日话题）。

- 交互说明：若用户从非互动场景进入授权，轻动作按钮替换为 [ 去广场看看 ]；页面展示一次后不再重复。

### 5.1.15 醒书活动 - 活动列表页（v2.1 新增）

- 页面布局：顶部标题“醒书活动” + 状态分组标签（近期预告 / 进行中 / 往期回顾）+ 活动卡片列表。

- 活动卡片：封面图（16:9）、活动标题、时间、形式标识（线上/线下+城市）、报名状态角标（可报名/名额已满/已截止/回顾）、已报名人数；卡片风格延续暖纸质感，与日记卡片视觉统一。

- 交互说明：所有身份可浏览列表；点击卡片进入详情——未完成微信授权的用户先拉起小程序微信授权（不涉及手机号），授权后进入；下拉刷新、上拉加载更多；空状态展示“活动筹备中，敬请期待”。

### 5.1.16 醒书活动 - 活动详情与报名页（v2.1 新增）

- 页面布局：封面图 + 活动信息区（标题、时间、地点/线上入口、组织方、名额与已报名人数）+ 图文详情区（富文本+图片）+ 底部固定报名栏。

- 报名交互：点击“报名参加”→ 已微信授权用户直接进入报名表单（字段由运营配置，联系方式为选填，**不强制手机号**）→ 提交后展示报名成功确认（含活动时间提醒建议）；已报名用户底部栏变为“已报名 · 取消报名”；名额满/截止后按钮置灰并显示对应文案。

- 往期回顾形态：已结束活动的详情页顶部标注“活动回顾”，正文为运营发布的图文回顾（照片墙 + 文字），无报名栏，底部可放“查看近期活动”引导。

- 海报分享（v2.2 新增）：详情页顶部提供分享入口，弹出活动海报（封面、标题、时间地点、报名状态、右下角带参小程序码——scene 携带活动 ID + 分享人用户 ID），支持保存图片/转发微信；扫码新用户绑定分享人为推荐人。

- 交互说明：报名成功/取消均有即时反馈；活动信息变更（时间/地点）时已报名用户进入详情页可见更新标注。

## 5.2 管理后台页面说明

### 5.2.1 首页 - 数据概览页

- 页面布局：顶部标题“数据概览” + 中间核心指标卡片 + 底部数据图表区域。

- 核心指标卡片：展示平台关键数据，包含：总用户数、会员数、日记总数、总点赞数、总收藏数、总评论数、总转发数，每个指标卡片包含数值、同比/环比变化（可选）。

- 数据图表区域：展示核心数据变化趋势，包含：用户增长趋势图（按时间）、日记发布趋势图（按时间）、互动数据趋势图（按时间），支持切换时间范围（日/周/月/年），支持下载图表。

- 交互说明：数据每10分钟自动刷新一次，支持手动刷新；点击指标卡片，跳转至对应的数据详情页（如点击总用户数，跳转至用户管理列表页）；图表支持hover查看具体数值。

### 5.2.2 用户管理 - 用户列表页

- 页面布局：顶部搜索/筛选栏 + 中间用户列表 + 底部分页控件。

- 搜索/筛选栏：支持按用户ID、手机号、身份类型（未授权/已授权/会员）、注册时间范围筛选，设置“重置”“搜索”按钮；支持批量导出用户列表（Excel格式）。

- 用户列表：表格形式，列包含：用户ID、手机号、昵称、身份类型、会员有效期（非会员显示“无”）、注册时间、操作（查看详情按钮）。

- 分页控件：支持设置每页显示条数（10/20/50条），支持上一页、下一页、跳转至指定页码。

- 交互说明：点击“查看详情”，跳转至用户详情页；筛选条件确认后，列表实时刷新并重置分页；导出按钮点击后，生成并下载Excel文件。

### 5.2.3 用户管理 - 用户详情页

- 页面布局：顶部返回按钮 + 中间用户基础信息区域 + 底部用户相关数据区域。

- 用户基础信息区域：展示用户详细信息，包含：用户ID、手机号、昵称、头像（默认/上传）、身份类型、会员有效期、注册时间、最后活跃时间、**推荐人（v2.2，附「修改」按钮）**。

- 修改推荐人（v2.2 新增）：点击「修改」弹出用户选择弹窗（按昵称/手机号搜索），可选定新推荐人或清空；提交校验：不能选用户本人、不得与对方形成互为推荐的循环；成功后即时生效并写入 admin_logs（记录旧值→新值与操作时间）。

- 用户相关数据区域：分为三个标签页，“发布日记”（展示该用户发布的所有日记，表格形式，包含日记ID、标题、发布时间、权限、互动数据）、“互动记录”（展示该用户的所有点赞、收藏、评论、转发记录，按时间排序）、**“他推荐的用户”（v2.2，展示以该用户为推荐人的用户列表）**。

- 交互说明：点击日记ID/标题，跳转至日记详情页；点击互动记录中的日记相关信息，跳转至对应日记详情页；点击返回按钮，返回用户列表页并保持筛选条件和分页位置。

### 5.2.4 日记管理 - 日记列表页

- 页面布局：顶部搜索/筛选栏 + 中间日记列表 + 底部分页控件。

- 搜索/筛选栏：支持多条件组合筛选，包含：日记ID、标题/内容关键词、标签、时间范围（发布时间）、作者昵称、权限类型（公众/会员/自己）、互动数据范围（点赞数/收藏数/评论数/转发数），设置“重置”“搜索”按钮；支持批量删除日记、批量导出日记列表。

- 日记列表：表格形式，列包含：日记ID、标题、作者昵称、发布时间、标签、权限类型、点赞数、收藏数、评论数、转发数、操作（查看详情、删除按钮）。

- 分页控件：与用户列表页一致，支持自定义每页条数和页码跳转。

- 交互说明：点击“查看详情”，跳转至日记详情页；点击“删除”，弹出确认弹窗，确认后删除日记及相关互动数据，提示“删除成功”；筛选条件确认后，列表实时刷新；批量操作需先勾选日记，再点击批量按钮。

### 5.2.5 日记管理 - 日记详情页

- 页面布局：顶部返回按钮 + 中间日记基础信息区域 + 底部互动数据区域。

- 日记基础信息区域：展示日记完整内容，包含：日记ID、标题（加粗）、作者昵称（可点击跳转至用户详情页）、发布时间、标签、权限类型、完整内容（分段展示）。

- 互动数据区域：分为四个标签页，“点赞记录”“收藏记录”“评论记录”“转发记录”，每个标签页以表格形式展示对应互动数据，包含：互动ID、用户ID、用户名、互动时间，支持筛选和导出。

- 交互说明：点击作者昵称，跳转至用户详情页；点击互动记录中的用户ID，跳转至对应用户详情页；点击返回按钮，返回日记列表页并保持筛选条件和分页位置；支持删除违规评论（评论记录页操作）。

### 5.2.6 互动数据管理 - 各数据页（点赞/收藏/评论/转发）

- 页面布局：统一布局，顶部搜索/筛选栏 + 中间数据列表 + 底部分页控件。

- 筛选条件：支持按日记ID、日记标题、用户ID、用户昵称、时间范围筛选，评论记录额外支持按评论内容筛选。

- 数据列表：表格形式，包含对应互动的核心信息（如评论记录包含：评论ID、日记ID、日记标题、用户ID、用户名、评论内容、评论时间、回复对象（二级回复）、操作（删除））。

- 交互说明：与日记列表页一致，支持筛选、分页、导出、单个/批量删除（评论记录支持）；点击日记ID/标题，跳转至日记详情页；点击用户ID/用户名，跳转至用户详情页。

# 六、UI 配色与视觉规范

## 6.1 整体视觉风格

整体采用简约、清新、文艺的风格，贴合日记记录的情感属性，避免复杂装饰，注重内容可读性和操作便捷性；小程序以轻量化为主，管理后台以简洁高效为主，保持视觉统一性。

## 6.2 小程序配色规范

### 6.2.1 主色调

- 主色：#4A90E2（淡蓝色），用于顶部导航、按钮、图标高亮、重点文字，传递清新、舒适的氛围，贴合日记的治愈感。

- 辅助色：

    - #FF6B6B（淡红色）：用于点赞按钮高亮、删除按钮、提示错误信息。

    - #FFD166（淡黄色）：用于收藏按钮高亮、标签底色、提示警告信息。

    - #06D6A0（淡绿色）：用于操作成功提示、会员标识、转发按钮高亮。

- 中性色：

    - #333333：用于标题、重点文字，保证可读性。

    - #666666：用于普通文字、作者昵称、时间，柔和不刺眼。

    - #999999：用于提示文字、次要信息，区分层级。

    - #F5F7FA：用于页面背景、卡片底色，提升整体清爽感。

    - #E5E5E5：用于分割线、边框，区分不同区域。

### 6.2.2 各模块配色细节

- 日记卡片：底色#FFFFFF，边框#E5E5E5，标题#333333，作者/时间#999999，内容摘要#666666，标签底色#F5F7FA、文字#4A90E2，权限标识：公众#06D6A0、会员#FFD166、自己#999999。

- 按钮：

    - 主要按钮（保存、支付）：底色#4A90E2，文字#FFFFFF，点击后底色加深。

    - 次要按钮（重置、取消）：底色#F5F7FA，文字#666666，边框#E5E5E5。

    - 图标按钮（点赞、收藏、评论、转发）：默认#999999，高亮/选中后对应辅助色。

- 会员中心：会员身份标签底色#FFD166、文字#333333，权益卡片底色#F5F7FA、边框#E5E5E5，数据卡片底色#FFFFFF、边框#E5E5E5，文字#333333。

- 弹窗：背景半透明#000000（透明度30%），弹窗底色#FFFFFF，标题#333333，内容#666666，按钮配色与上述一致。

## 6.3 管理后台配色规范

### 6.3.1 主色调

- 主色：#3578F6（深蓝色），用于左侧导航栏、顶部标题栏、按钮、重点数据，传递专业、高效的氛围。

- 辅助色：与小程序一致，用于提示信息、操作按钮（删除#FF6B6B、成功#06D6A0、警告#FFD166）。

- 中性色：

    - #1F2937：用于页面标题、表格标题、重点文字。

    - #4B5563：用于普通文字、表格内容。

    - #9CA3AF：用于提示文字、次要信息。

    - #F3F4F6：用于页面背景、表格交替行底色。

    - #E5E7EB：用于分割线、边框、表格边框。

### 6.3.2 各模块配色细节

- 左侧导航栏：底色#3578F6，选中项底色#2563EB，文字#FFFFFF，未选中项文字#E0E7FF。

- 顶部标题栏：底色#FFFFFF，文字#1F2937，边框#E5E7EB。

- 表格：底色#FFFFFF，边框#E5E7EB，表头底色#F3F4F6，文字#1F2937，表格内容文字#4B5563，交替行底色#F9FAFB，hover行底色#F3F4F6。

- 按钮：主要按钮（搜索、导出）底色#3578F6，文字#FFFFFF；次要按钮（重置、查看）底色#F3F4F6，文字#4B5563，边框#E5E7EB；删除按钮底色#FF6B6B，文字#FFFFFF。

- 数据卡片：底色#FFFFFF，边框#E5E7EB，标题#9CA3AF，数据#1F2937，同比/环比文字：增长#06D6A0、下降#FF6B6B。

## 6.4 视觉通用规范

- 圆角：所有卡片、按钮、输入框、弹窗均采用8px圆角，避免尖锐边角，提升柔和感。

- 阴影：卡片、弹窗添加轻微阴影（阴影值：0 2px 8px rgba(0,0,0,0.08)），增强层次感，不突兀。

- 图标：统一采用线性图标，线条粗细2px，颜色与对应文字/按钮颜色一致，风格简约统一；小程序图标尺寸24px，管理后台图标尺寸20px。

- 文字：

    - 小程序：标题（18px，加粗）、正文（16px）、次要文字（14px）、提示文字（12px），字体采用“微信官方字体”（sans-serif fallback）。

    - 管理后台：标题（20px，加粗）、表格标题（16px，加粗）、正文/表格内容（14px）、提示文字（12px），字体采用“系统默认字体”（sans-serif fallback）。

- 间距：统一采用8px、16px、24px作为基础间距，页面内间距均匀，避免拥挤或松散，提升整洁度。

# 七、交互与状态设计

## 7.1 通用交互规范

- 反馈机制：所有操作（点击、提交、删除、授权等）均需提供明确反馈，如按钮点击震动、操作成功/失败提示弹窗（3秒自动消失）、加载动画（操作耗时超过1秒时显示）。

- 跳转逻辑：返回按钮统一返回上一页，保持页面层级清晰；跳转至详情页、编辑页等，返回时保持原页面的筛选条件、分页位置、滚动位置，提升用户体验。

- 手势操作：支持下拉刷新（列表页）、上拉加载更多（列表页）、左右滑动切换标签页（管理后台、会员中心）、点击空白处关闭弹窗（筛选弹窗、评论弹窗等）。

- 输入交互：输入框获取焦点时，边框高亮（主色）；输入错误/超出限制时，提示文字变红并显示具体错误信息；输入完成后，点击回车/确认按钮触发提交。

## 7.2 小程序核心交互设计

### 7.2.1 日记相关交互

- 新建/编辑日记：输入标题和内容后，保存按钮激活；未输入时，点击保存无反应并提示“请输入标题和内容”；保存成功后，提示并跳转至对应列表页；取消编辑时，有内容则弹出确认弹窗。

- 日记删除：点击删除按钮，弹出确认弹窗（“确定删除该日记吗？删除后不可恢复”），确认后删除并提示，列表实时刷新；删除失败时，提示“删除失败，请重试”。

- 日记查看：点击日记卡片跳转至详情页，返回时列表保持上次浏览位置，并自动刷新互动数据（点赞、收藏等）；详情页支持上下滑动查看内容和评论。

- 日记搜索/筛选：输入关键词/选择筛选条件后，点击确认，列表实时刷新并定位至顶部；无符合条件的内容时，显示“暂无相关日记”提示文字（居中，灰色，搭配空状态图标）。

### 7.2.2 互动相关交互

- 点赞/取消点赞：点击点赞按钮，图标立即变色，数量实时+1/-1，无其他弹窗（避免干扰）；未授权用户点击，跳转至授权页。

- 收藏/取消收藏：点击收藏按钮，图标立即变色，数量实时+1/-1，收藏时弹出短暂提示“已收藏”，取消时提示“已取消收藏”；未授权用户点击，跳转至授权页。

- 评论/回复：点击评论按钮，弹出评论输入框并聚焦，输入内容后点击发送，评论实时添加至列表顶部，数量+1；点击回复按钮，显示二级回复列表并弹出回复输入框，回复内容添加至对应评论下方；删除自己的评论/回复，点击删除按钮，弹出确认弹窗，确认后删除，数量实时-1；未授权用户点击，跳转至授权页。

- 转发：点击转发按钮，跳转至海报生成页，加载过程中显示加载动画；海报生成后，支持保存图片和分享，保存成功提示“图片已保存至相册”，分享后返回详情页。

### 7.2.3 会员相关交互

- 会员购买：点击开通/续费会员，跳转至购买页，点击支付按钮，调用微信支付接口；支付成功后，跳转至会员中心，提示“会员开通/续费成功”，同步更新会员身份和有效期；支付失败/取消，提示“支付失败，请重试”/“已取消支付”，返回购买页。

- 权限管控（v2 调整为内容墙渐隐）：未授权用户点击需授权的功能，跳转至验证页（经欢迎时刻页）后返回原页面并自动完成对应操作；非会员打开会员权限日记，**正常进入详情页并展示前约 30% 内容，随后渐隐**，渐隐区叠加文案“完整内容向会员开放”+ [了解会员] 入口（跳转会员购买页）；会员查看会员权限日记，无额外提示，正常展示全文。渐隐截断在服务端完成，非会员的接口返回中不包含被截断的内容。

## 7.3 管理后台核心交互设计

- 数据筛选/搜索：选择筛选条件/输入搜索关键词后，点击搜索，列表实时刷新并重置分页；无符合条件的数据时，显示“暂无相关数据”提示；筛选条件支持记忆，返回页面时保留上次筛选条件。

- 数据导出：点击导出按钮，弹出提示“正在生成文件，请稍候”，生成完成后自动下载Excel文件；导出失败时，提示“导出失败，请重试”。

- 内容删除：删除日记、评论等内容时，弹出确认弹窗，确认后删除并提示“删除成功”，列表实时刷新；批量删除时，需先勾选内容，再点击批量删除按钮，确认后批量删除。

- 分页交互：点击上一页/下一页，跳转至对应页码，列表平滑刷新；输入页码并回车，跳转至指定页码；每页条数切换后，列表重置为第一页并刷新。

- 数据刷新：管理后台数据每10分钟自动刷新一次，支持手动点击刷新按钮刷新；刷新过程中，数据区域显示加载动画，不影响其他操作。

## 7.4 状态设计

### 7.4.1 小程序状态设计

- 页面状态：正常状态：页面加载完成，所有元素（列表、按钮、输入框等）正常显示，可正常操作，数据完整展示。

- 加载状态：页面初始化、下拉刷新、上拉加载、筛选搜索、海报生成时，显示轻量加载动画（如圆形旋转图标），加载区域占位，避免页面抖动；加载耗时超过3秒时，添加“加载中，请稍候”文字提示。

- 空状态：列表页（广场、我的收藏、我的日记）无符合条件内容、互动记录为空时，显示空状态图标+提示文字（如“暂无相关日记”“暂无收藏内容”“暂无互动记录”），提示文字为灰色，居中展示，下方可添加引导按钮（如“去新建日记”“去广场发现”）。

- 异常状态：网络异常、接口请求失败时，显示异常图标+提示文字（“网络不佳，请检查网络后重试”“请求失败，请刷新重试”），居中展示，添加“刷新”按钮，点击可重新发起请求；无权限访问（如普通游客查看会员日记）时，显示权限不足提示+开通会员入口/返回按钮。

- 操作状态：
            未选中/未操作状态：按钮、图标（点赞、收藏等）显示默认颜色，输入框未获取焦点，弹窗未弹出，所有元素处于可触发状态。

- 选中/操作中状态：按钮点击时底色加深、图标轻微放大（反馈效果）；输入框获取焦点时边框高亮（主色），显示光标；点赞/收藏选中后图标显示对应辅助色，数量实时更新；筛选弹窗弹出时，遮罩层显示，弹窗内选中项高亮。

- 禁用状态：未授权用户的新建、点赞、收藏等按钮，未输入标题内容的保存按钮，处于禁用状态（置灰、无点击反馈），鼠标悬浮时可显示禁用提示（如“请授权手机号解锁功能”“请输入标题和内容”）。

- 操作成功/失败状态：操作完成后弹出对应提示弹窗（3秒自动消失），成功提示为绿色图标+“操作成功”（如“发布成功”“已取消收藏”），失败提示为红色图标+具体失败原因（如“删除失败，请重试”“保存失败，网络异常”），不干扰用户后续操作。

- 用户身份状态：
            未授权手机号游客（v2.1 修订）：会员中心显示“未授权手机号游客”身份标签；可浏览日记列表（标题/摘要）与活动列表；**点击任意日记详情**或互动功能时，跳转至手机号验证页；活动详情/报名仅需微信授权（不涉及手机号）。

- 已授权手机号游客：会员中心显示“已授权手机号游客”身份标签，可正常使用基础功能，无法查看会员权限日记，会员购买入口显示“开通会员”。

- 会员用户：会员中心显示“会员”身份标签+会员有效期，解锁所有功能，可查看会员权限日记，日记卡片、个人身份标签显示会员专属标识，会员购买入口显示“续费会员”。

- 日记权限状态：
           公众权限（v2.1 修订）：日记卡片显示“公众”标识（绿色图标+文字），列表卡片所有用户可见；**详情需已授权（手机号验证）用户方可查看**，未授权用户点击详情触发验证引导；互动功能需已授权。

- 会员权限（v2 调整）：日记卡片显示“会员”标识（黄色图标+文字），会员可读全文；非会员可读前约 30% 后渐隐，渐隐处展示会员开通引导（软筛选，不弹窗拦截）。

- 自己权限：日记卡片显示“私密”标识（灰色图标+文字），仅日记作者可查看、编辑、删除，其他用户无法在列表中看到该日记。

### 7.4.2 管理后台状态设计

- 页面状态：
            正常状态：页面加载完成，表格、图表、按钮、筛选栏等元素正常显示，数据完整渲染，可正常进行筛选、导出、删除等操作。

- 加载状态：页面初始化、数据筛选、搜索、导出、详情加载时，显示加载动画（表格区域占位加载、按钮加载状态），表格区域可显示“加载中...”文字提示；大数据量加载时，添加进度提示（如“加载中，已完成30%”）。

- 空状态：用户列表、日记列表、互动数据列表等无符合条件数据时，表格区域显示“暂无相关数据”提示文字（居中、灰色），无多余装饰，筛选栏保持可操作状态。

- 异常状态：网络异常、接口请求失败、权限不足时，显示异常提示（“网络异常，请检查网络后重试”“请求失败，请刷新页面”“您无此操作权限”），搭配对应图标，表格区域显示异常占位，添加“刷新”按钮（网络/请求异常时）或“返回上一页”按钮（权限不足时）。

- 分页状态：当前页码高亮显示，首页时“上一页”按钮禁用（置灰），末页时“下一页”按钮禁用（置灰）；无数据时，分页控件隐藏。

- 操作状态：
            未操作/未选中状态：按钮、筛选框、复选框等处于默认状态，复选框未勾选，按钮可点击，表格行无hover效果。

- 操作中状态：按钮点击后显示加载状态（按钮内旋转图标+文字“处理中...”），不可重复点击；筛选条件选择时，下拉框展开、复选框勾选状态实时切换；表格行hover时，底色变为浅灰色（贴合视觉规范），便于区分当前操作行。

- 禁用状态：无权限操作的按钮（如普通运营者的批量删除按钮）、未勾选数据时的批量操作按钮，处于禁用状态（置灰、无点击反馈），鼠标悬浮时显示禁用提示（如“无此操作权限”“请选择需操作的数据”）。

- 操作成功/失败状态：删除、导出、批量操作等完成后，页面顶部弹出提示条（左对齐，3秒自动消失），成功提示为绿色+“操作成功”（如“删除成功”“导出成功，文件已下载”），失败提示为红色+具体失败原因（如“删除失败，请重试”“导出失败，数据量过大”）；重要操作（如批量删除日记）失败时，可弹出确认弹窗提示是否重新操作。

- 数据状态：
            正常数据状态：表格、图表数据完整渲染，数值清晰，标签、权限等标识准确显示（与小程序端一致），图表hover时显示具体数据详情。

- 异常数据状态：数据缺失（如用户手机号为空、日记内容为空）时，表格对应单元格显示“无”（灰色）；数据异常（如互动数为负数）时，对应单元格显示红色数值，便于运营者排查。

- 筛选状态：筛选条件选择后，筛选栏显示已选条件（可单个删除），表格数据实时刷新，筛选状态持续保留，直至点击“重置”按钮清空。
# 八、会员转化与筛选运营策略（v2 新增）

> 本章为《醒书日记的会员转化策略设计》在 PRD 中的落地总纲，是第三至七章 v2 变更点的策略依据。详细论证见 doc/醒书日记的会员转化策略设计.md。

## 8.1 核心定位：筛选而非转化

会员机制的目标不是付费人数最大化，而是用付费与社区行为两道筛子，沉淀"有爱心、热情、责任心、上进心、愿意付出"的人群：

- 第一道粗筛（付费=投名状）：愿意付费的人已用真金白银证明"我是认真的"；
- 第二道精筛（社区行为）：付费之后，通过"付出型互动"行为数据（认真回应他人、鼓励新人、传递温暖）识别真正的目标成员，授予荣誉身份。

## 8.2 三层转化漏斗

```
公域引流（公众号/视频号）        小程序承接（筛选核心战场）           会员沉淀
用价值观而非功能吸引      →    游客：体验 — 认同 — 决策      →    认同驱动付费
"如果你也愿意认真生活，        Step0 落地欢迎屏（气质）             年卡 365 = 一年的承诺
 这里有一群同路人"             Step1 沉浸阅读（编辑精选）           创作激励反哺会员
                              Step2 互动触发验证（仪式感）
                              Step3 欢迎时刻（即时正反馈）
                              Step4 今日话题 + 首篇创作（临门一脚）
```

情绪节奏：好奇（落地）→ 认同（阅读）→ 冲动（想互动）→ 归属（验证+欢迎）→ 成就（首篇发布）。整个承接阶段**不提会员、不提付费**——付费转化是下一个漏斗的事。

> v2.1 权限收紧后的说明：未授权用户的"沉浸阅读"（Step1）发生在**列表卡片（标题+摘要）与落地欢迎屏精选卡片**层面；点击任意日记详情即触发验证（Step2 前移）。因此列表摘要与欢迎屏精选的策展质量，直接决定 Step1→Step2 的转化效率。醒书活动栏目（微信授权即可看详情/报名）是另一条更低门槛的认同建立路径，与日记漏斗互补。

## 8.3 关键设计原则（反常识提醒）

1. **越想筛选优质用户，前期越要"不功利"**：目标人群对套路高度敏感，任何急于转化的动作（弹窗、诱导、红点）都会赶走真正想要的人。克制是对这群人最好的尊重。
2. **"首篇创作"是北极星先行指标**：写过第一篇的人才真正进入漏斗，此指标优先于注册转化率。
3. **首篇默认"仅自己可见"**：先让人敢写、开始写，公开分享是归属感建立后的自然结果。
4. **官方的"第一次回应"是性价比最高的投入**：1 小时内的真诚点赞+评论，是把"愿意付出"的人留下来的关键钩子。
5. **内容墙做软筛选**：会员日记"预览 30% + 渐隐"转化被内容打动的人，而非被功能限制逼迫的人。

## 8.4 漏斗指标体系

| 转化节点 | 指标 | 说明 |
|----------|------|------|
| 落地→阅读 | 游客阅读时长与深度 | 是否被内容吸引 |
| 阅读→授权 | 游客→授权用户转化率 | 认同感建立 |
| 授权→创作 | **授权用户→首篇创作转化率** | **北极星先行指标** |
| 创作→付费 | 创作者→付费会员转化率 | 最终变现 |
| 付费→留存 | 会员续费率 | 社区粘性的终极验证 |

## 8.5 风险提示

付费只能筛选"愿意为认同买单的人"，买单不等于有爱心。真正的品质筛选发生在付费之后的社区行为里——运营管理中的"付出型互动观察"（3.1.6/3.2）是本策略闭环的必要组成，不可省略。
